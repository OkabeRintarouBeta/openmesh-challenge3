python script to extract data from the source and store in s3
python script to store data from s3 to Postgres RDS
SQL query to transform data into crypto schema in a more normalized and structured way
